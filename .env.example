DATABASE_URL="postgresql://user:password@localhost:5432/readymiix_cabana"
SESSION_SECRET="replace-with-a-long-random-string"

# Facultatif : active GET /api/seed?token=... pour initialiser une base vide
# (catégories, produits de démo, comptes) sans accès direct à la base de
# données. À supprimer une fois la base initialisée.
SEED_TOKEN=""
